<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent" >

    <TableLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:background="@color/share_bg" >

        <include layout="@layout/custom_title_no_playing" />

        <RelativeLayout
            android:id="@+id/relativeLayout1"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content" >

            <ListView
                android:id="@+id/accout_list"
                android:layout_width="fill_parent"
                android:layout_height="wrap_content"
                android:layout_alignParentTop="true"
                android:layout_margin="5dp"
                android:cacheColorHint="#00000000" >
            </ListView>

            <TextView
                android:id="@+id/point_hint"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_below="@id/accout_list"
                android:layout_marginTop="36dp"
                android:padding="5dp"
                android:text="绑定合作网站后，可以方便的分享内容给该网站的好友们。首次绑定还可获赠50扇贝！"
                android:textAppearance="?android:attr/textAppearanceSmall" />

            <Button
                android:id="@+id/get_award"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_below="@id/point_hint"
                android:layout_centerHorizontal="true"
                android:layout_marginTop="40dp"
                android:background="@drawable/button_bg"
                android:padding="8dp"
                android:text="领取奖励"
                android:textSize="18sp" />
        </RelativeLayout>
    </TableLayout>

    <include layout="@layout/night_mask" />

</RelativeLayout>